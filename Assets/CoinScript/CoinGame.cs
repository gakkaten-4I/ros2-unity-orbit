/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine;
using UnityEngine.AI;

public class CoinGame : MonoBehaviour
{
    // Start is called before the first frame update
    
    //双方の取得エフェクト
    //public gameObject get_red;
    //public gameObject get_blue;

    //ステージの番号
    public int stageNum;
 
    private int sumPoint;
    private int timeCount;
    private list<int> maxpoint = {10,9,8,9,13};

    void Start()
    {
        restartGame();
    }


    //それぞれのエリアでのゲームの開始時にカウント用のポイントをリセットする。
    void restartGame(){ 
        sumPoint = 0;
        timeCount = 0;
    }

    //コインのカウントをするメソッド

    // Update is called once per frame
    void Update()
    {
        if(timeCount >= 600 or sumPoint >= )
    }
}
*/